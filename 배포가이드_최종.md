# 🎯 Boostar Vercel 배포 - 3단계 완벽 가이드

## 준비 완료 ✅
- ✅ Git 저장소 초기화됨
- ✅ 모든 코드 커밋 완료
- ✅ 빌드 테스트 통과
- ✅ Vercel CLI 설치됨

---

## 🚀 배포 3단계

### 1️⃣ GitHub에 저장소 만들기 (2분)

1. **브라우저에서 열기**: https://github.com/new
2. **로그인** (이미 로그인되어 있다면 Skip)
3. **Repository name**: `boostar-dev` 입력
4. **Public** 선택 (또는 Private)
5. ⚠️ **중요**: "Add a README file" 체크 **해제**
6. **"Create repository"** 버튼 클릭

7. **GitHub가 제공하는 명령어 복사**
   - 페이지에 "...or push an existing repository from the command line" 섹션이 보입니다
   - 아래 3줄을 복사하세요:
   ```
   git remote add origin https://github.com/YOUR_USERNAME/boostar-dev.git
   git branch -M main
   git push -u origin main
   ```

8. **PowerShell에 붙여넣기 (이 프로젝트 폴더에서)**
   ```powershell
   # GitHub가 제공한 명령어를 여기에 붙여넣기
   ```

9. GitHub 인증 창이 뜨면 로그인

✅ 코드가 GitHub에 업로드되었습니다!

---

### 2️⃣ Vercel에서 배포하기 (3분)

1. **브라우저에서 열기**: https://vercel.com/new

2. **GitHub로 로그인** (또는 이미 로그인되어 있다면 Skip)

3. **"Import Git Repository"** 섹션에서:
   - 방금 만든 `boostar-dev` 저장소를 찾습니다
   - 보이지 않으면 "Adjust GitHub App Permissions" 클릭하여 권한 추가
   - **"Import"** 버튼 클릭

4. **프로젝트 설정 확인** (자동으로 감지됨):
   ```
   Framework Preset: Next.js ✓
   Root Directory: ./ ✓
   Build Command: npm run build ✓
   Output Directory: .next ✓
   ```

5. **환경 변수 추가** (⚠️ 매우 중요!):
   - "Environment Variables" 섹션 펼치기
   
   **첫 번째 변수:**
   - Name: `NEXT_PUBLIC_SUPABASE_URL`
   - Value: (Supabase 프로젝트 URL 입력)
     - Supabase Dashboard → Settings → API → Project URL
     - 예: `https://xxxxxxxxxxxxx.supabase.co`
   
   **두 번째 변수:**
   - Name: `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - Value: (Supabase Anon Key 입력)
     - Supabase Dashboard → Settings → API → Project API keys → anon public
     - 예: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`

6. **"Deploy"** 버튼 클릭!

⏱️ 배포 진행 중... (2-3분 소요)

✅ 배포 완료!

---

### 3️⃣ 확인 및 테스트 (1분)

1. **배포 완료 화면에서**:
   - "Visit" 버튼 클릭 또는
   - 제공된 URL 클릭 (예: `https://boostar-dev.vercel.app`)

2. **기능 테스트**:
   - ✅ 페이지가 정상적으로 로드되는지
   - ✅ 로그인/회원가입 버튼이 보이는지
   - ✅ 로그인 시도 (Supabase 연결 확인)

3. **문제가 있다면**:
   - Vercel Dashboard → 해당 프로젝트 선택
   - Settings → Environment Variables
   - 환경 변수가 올바른지 확인
   - Deployments → "Redeploy" 클릭

---

## 🎉 완료!

축하합니다! 프로젝트가 성공적으로 배포되었습니다!

### 이제 자동으로:
- ✅ Git push하면 자동 재배포
- ✅ HTTPS 자동 적용
- ✅ 전 세계 CDN 배포
- ✅ Preview URL로 테스트 가능

### 배포된 URL:
```
https://boostar-dev.vercel.app
```
(실제 URL은 Vercel이 제공합니다)

---

## 📝 커맨드 치트시트

### GitHub에 푸시 (최초 1회)
```powershell
# 본인의 GitHub 저장소 URL로 변경
git remote add origin https://github.com/YOUR_USERNAME/boostar-dev.git
git branch -M main
git push -u origin main
```

### 이후 코드 변경 시
```powershell
git add .
git commit -m "Update: 변경 내용"
git push
```
→ 자동으로 Vercel에 재배포됩니다!

---

## 🆘 문제 해결

### Q: GitHub 인증이 안 돼요
**A**: GitHub Desktop 사용 권장
1. https://desktop.github.com 설치
2. GitHub 로그인
3. File → Add Local Repository
4. 현재 폴더 선택
5. Publish repository

### Q: 환경 변수를 잊어버렸어요
**A**: 
1. Supabase Dashboard → Settings → API
2. Project URL과 anon public key 복사
3. Vercel → Settings → Environment Variables 추가
4. Redeploy

### Q: 배포는 되는데 오류가 나요
**A**:
1. Vercel Dashboard → Deployments → Functions 탭
2. 에러 로그 확인
3. 환경 변수 누락 확인

---

## 📞 준비된 것들

프로젝트 폴더에 다음 파일들이 있습니다:
- `DEPLOY_MANUAL_GUIDE.md` - 상세 가이드
- `vercel.json` - Vercel 설정 파일
- `deploy.ps1` - 배포 스크립트 (CLI 대신)

**이제 위의 3단계만 따라하시면 됩니다!** 🚀

